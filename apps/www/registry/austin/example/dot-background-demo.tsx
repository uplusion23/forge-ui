import { Card } from "@/registry/austin/ui/card"
import { DotBackground } from "@/registry/austin/ui/dot-background"

export default function LabelDemo() {
  return (
    <Card>
      <DotBackground width={20} height={20} cx={1} cy={1} cr={1} />
    </Card>
  )
}
